<odoo>

    <record id="asset_sell_partial_view" model="ir.ui.view">
        <field name="name">wizard.view.partial.sell.assets.form</field>
        <field name="model">account.asset.sell</field>
        <field name="inherit_id" ref="account_asset.asset_sell_form"/>
        <field name="arch" type="xml">
            <field name="action" position="after">
                <field name="leasee_asset_bool" invisible="1"/>
                <field name="partial_bool" attrs="{'invisible': [('leasee_asset_bool', '=', True)]}" />
                <field name="partial_amount" attrs="{'invisible': [('partial_bool', '=', False), ('leasee_asset_bool', '=', True)]}"/>
            </field>
        </field>
    </record>

</odoo>