<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record model="ir.rule" id="budget_in_out_multi_company_rule">
            <field name="name">Budget In Out multi company</field>
            <field name="model_id" ref="model_budget_in_out_check_invoice"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            </field>
        </record>
        <record model="ir.rule" id="budget_in_out_lines_multi_company_rule">
            <field name="name">Budget In Out multi company</field>
            <field name="model_id" ref="model_budget_in_out_lines_invoice"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            </field>
        </record>

    </data>
</odoo>