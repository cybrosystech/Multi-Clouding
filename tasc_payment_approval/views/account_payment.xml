<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_account_payment_form" model="ir.ui.view">
        <field name="name">
            account.payment.form.inherited.tasc.payment.approval
        </field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position='after'>
                <field name="payment_approval_batch_id" invisible="1"/>
                <field name="payment_approval_status" invisible="1"/>
                <field name="invoice_number" invisible="1"/>
                <field name="invoice_amount" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="view_account_payment_tree" model="ir.ui.view">
        <field name="name">account.supplier.payment.tree</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_tree"/>
        <field name="arch" type="xml">
            <field name="currency_id" position="after">
                <field name="payment_approval_batch_id"/>
                <field name="payment_approval_status"/>
            </field>
        </field>
    </record>
</odoo>